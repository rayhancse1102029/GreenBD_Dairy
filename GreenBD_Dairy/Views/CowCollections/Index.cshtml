@model IEnumerable<GreenBD_Dairy.Models.CowCollection>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.CowCollectionName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ManagerSignature)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.EntryDate)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CowCollectionName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ManagerSignature)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EntryDate)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CowCollectionId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CowCollectionId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CowCollectionId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
